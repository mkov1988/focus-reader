# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Heavy design assets — kept local, not needed in the repo
docs/UI Inspo/
docs/assets/references/

# Stray package tarballs / extracted packages
*.tgz
/package/

# Mirrored book covers — a build/staging output of `npm run mirror:covers`.
# Uploaded to the static cover host (VITE_COVER_BASE), not shipped in the app.
/covers/

# Lint run artifact
lint_output.txt
